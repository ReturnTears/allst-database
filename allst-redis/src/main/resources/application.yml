spring:
  redis:
    host: 127.0.0.1
    port: 6379
    password: #123456(home)，company is null
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 10
        min-idle: 0
    lettuce:
      shutdown-timeout: 0
    database: 0
    timeout: 1000
